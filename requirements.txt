# Dependencies for hatun-mcp
# Bumped 2026-06-03 by Auditor BETA (Beta-iter8)
# Versions verified from PyPI API 2026-06-03
mcp>=1.28.0,<2          # was >=1.10; latest 1.27.2
httpx>=0.28.1         # already current (0.28.1)
starlette>=1.3.1        # was >=0.37; latest 1.2.1 — SECURITY: major bump
uvicorn[standard]>=0.49.0  # already current (0.48.0)
cryptography>=49.0.0      # was >=42; latest 48.0.0
py_ecc>=8.0.0           # BLS12-381 aggregate signatures (real curve; honest Merkle fallback if absent)
pytest>=9.1.1           # test suite (test_server, test_quorum, test_adapters, test_governance)
pytest-asyncio>=1.4.0  # async test support
